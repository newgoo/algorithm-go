package main

import "github.com/newgoo/algorithm"

func Shell(list []int64, steps ...int64) {
	var h = 1
	var length = len(list)
	if h < length {

	}
}

func main() {
	list := algorithm.Random(1000)
	Shell(list, 15, 8, 2, 1)
}
